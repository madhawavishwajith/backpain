import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Back Pain Relief & Recovery Solutions",
  description: "Understanding, treating, and preventing back pain through modern medical solutions and expert care. Explore our interactive 3D spine model and comprehensive treatment options.",
  keywords: "back pain, spine health, pain relief, treatment, prevention, 3D spine model",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
